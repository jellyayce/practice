function QuizStart({handleStartButton}){
    return(
        <>
            <div>
                <button onClick={handleStartButton}>Start Quiz</button>
            </div>
        </>
    );
}
export default QuizStart